// Supervisor v1 — drift detection
// Reads: FTS5 index + identity files + temporal state
// Checks: temporal anchor correctness, capability staleness, factual contradictions
// Writes: drift-report.md
